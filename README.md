# Онлайн-анкетирование по здоровью

Современный, быстрый, профессиональный сайт онлайн-анкетирования в сфере здоровья с поддержкой нескольких типов анкет, мультиязычностью, загрузкой файлов и автоматической отправкой в Telegram.

## 🚀 Технологии

- **Next.js 14** (App Router)
- **TypeScript**
- **Tailwind CSS**
- **Framer Motion** (лёгкие анимации)
- **React Hook Form** + **Zod** (валидация форм)
- **Vercel** (деплой)

## 📋 Типы анкет

1. **Женская анкета** - `/questionnaire/women`
2. **Мужская анкета** - `/questionnaire/men`
3. **Анкета для младенца (до 1 года)** - `/questionnaire/infant`
4. **Детская анкета** - `/questionnaire/child`
5. **Анкета по рекомендации** - `/questionnaire/recommendation`

## 🌍 Мультиязычность

Поддержка русского и английского языков с переключателем в навигации. Выбранный язык сохраняется в localStorage и передаётся вместе с анкетой в Telegram.

## 📤 Отправка в Telegram

После заполнения анкета автоматически отправляется в указанную группу Telegram через Bot API. Все прикреплённые файлы также отправляются в группу.

## ⚙️ Установка и настройка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=8532495259:AAFO3Jq2q9q0aVyONurT7WlBS2u5fgfbl-E
TELEGRAM_CHAT_ID=-1003794327640
```

**Важно:** Файл `.env.local` уже добавлен в `.gitignore` и не будет закоммичен в репозиторий.

### 3. Получение Telegram Bot Token

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `TELEGRAM_BOT_TOKEN`

### 4. Получение Chat ID

1. Добавьте бота в группу Telegram
2. Отправьте сообщение в группу
3. Откройте в браузере: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Найдите `chat.id` в ответе и скопируйте в `TELEGRAM_CHAT_ID`

### 5. Запуск проекта

```bash
# Разработка
npm run dev

# Продакшн сборка
npm run build
npm start
```

## 🚀 Деплой на Vercel

1. Подключите репозиторий к Vercel
2. Добавьте переменные окружения в настройках проекта (Settings → Environment Variables):
   - `TELEGRAM_BOT_TOKEN` = `8532495259:AAFO3Jq2q9q0aVyONurT7WlBS2u5fgfbl-E`
   - `TELEGRAM_CHAT_ID` = `-1003794327640`
3. Перезапустите деплоймент после добавления переменных
4. Деплой произойдёт автоматически при следующих коммитах

## 📁 Структура проекта

```
├── app/
│   ├── api/
│   │   └── submit/          # API endpoint для отправки анкет
│   ├── questionnaire/       # Страницы анкет
│   ├── layout.tsx           # Основной layout
│   ├── page.tsx            # Главная страница
│   └── globals.css         # Глобальные стили
├── components/             # React компоненты
│   ├── FileUpload.tsx      # Компонент загрузки файлов
│   ├── FormField.tsx       # Компонент поля формы
│   ├── LanguageSwitcher.tsx
│   ├── Navbar.tsx
│   └── ProgressBar.tsx
├── lib/
│   ├── i18n.ts            # Переводы
│   ├── locale.tsx         # Контекст языка
│   ├── questionnaire-schemas.ts  # Zod схемы
│   └── telegram.ts        # Логика отправки в Telegram
└── public/                # Статические файлы
```

## 🔐 Безопасность

- Все данные отправляются только в защищённую группу Telegram
- Файлы не сохраняются на сервере
- Минимальная обработка персональных данных
- Только HTTPS в продакшене

## ⚡ Производительность

- Оптимизированные изображения
- Lazy loading компонентов
- Минимальный размер JS бандла
- Лёгкие анимации (отключаются на слабых устройствах)

## 📝 Лицензия

Проект создан для использования в медицинской сфере.

